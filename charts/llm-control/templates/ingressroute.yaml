apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: llm-joysontech-com
  namespace: {{ .Values.namespace }}
spec:
  secretName: llm-joysontech-com-tls
  issuerRef:
    name: local-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - llm.joysontech.com
  duration: 8760h
  renewBefore: 720h
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: llm-control
  namespace: {{ .Values.namespace }}
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`llm.joysontech.com`)
      middlewares:
        - name: local-only
          namespace: traefik
      services:
        - name: llm-control
          port: {{ .Values.port }}
  tls:
    secretName: llm-joysontech-com-tls
