apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-config
  namespace: {{ .Values.namespace }}
  labels:
    app: openclaw
    team: platform
    env: {{ .Values.env }}
data:
  openclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "lan",
        "port": {{ .Values.gateway.port }},
        "auth": {
          "mode": "token"
        },
        "trustedProxies": ["10.244.0.0/16", "10.0.0.0/8"],
        "controlUi": {
          "enabled": true,
          "allowedOrigins": ["https://openclaw.joysontech.com"]
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.openclaw/workspace",
          "model": "{{ .Values.llm.provider }}"
        },
        "list": [
          {
            "id": "default",
            "name": "OpenClaw Assistant",
            "workspace": "~/.openclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
