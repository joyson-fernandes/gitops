apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: uptime-joysontech-com
  namespace: {{ .Values.namespace }}
spec:
  secretName: uptime-joysontech-com-tls
  issuerRef:
    name: local-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - uptime.joysontech.com
  duration: 8760h
  renewBefore: 720h
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: uptime-kuma
  namespace: {{ .Values.namespace }}
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`uptime.joysontech.com`)
      middlewares:
        - name: local-only
          namespace: traefik
        - name: strip-frame-options
      services:
        - name: uptime-kuma
          port: {{ .Values.port }}
  tls:
    secretName: uptime-joysontech-com-tls
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-frame-options
  namespace: {{ .Values.namespace }}
spec:
  headers:
    customResponseHeaders:
      X-Frame-Options: ""
